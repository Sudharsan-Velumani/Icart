services:
  icart:
    build:
      context: .
      args:
        REDIS_URL: ${REDIS_URL}
    ports:
      - "4321:4321"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=4321
    env_file:
      - .env
    restart: unless-stopped